import os


# Takes a directory full of images and videos, 
# turns them into a project, generates a gallery and uploads it.

def bootstrap():
    # would need: name, url, theme, upload details

if __name__=="__main__":
    bootstrap()
